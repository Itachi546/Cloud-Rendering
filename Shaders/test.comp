#version 450

layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

layout(binding = 0, rgba8) uniform image3D uInputTexture;

void main() {
   ivec3 uv = ivec3(gl_GlobalInvocationID.xyz);
   imageStore(uInputTexture, uv, vec4(vec3(uv) / 32.0f, 1.0f));
}