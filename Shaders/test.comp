#version 450

layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

layout(binding = 0, r32f) uniform image3D uInputTexture;

void main() {
   ivec3 uv = ivec3(gl_GlobalInvocationID.xyz);

   float color = uv.z / 256.0f;
   imageStore(uInputTexture, uv, vec4(color));
}